# Copyright (C) 2025 Tencent.
# BQLOG is licensed under the Apache License, Version 2.0.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.

"""
Generated Wrapper For test_category_log (Python)

A category_log that supports attaching a category to each log entry.
Categories can be used to filter logs within the appender settings.

Usage:
    my_log = test_category_log.create_log(name, cfg)
    my_log.info("no category")
    my_log.info(my_log.cat.ModuleA.SystemA, "with category {}", "ok")
"""

from bq.category_log import category_log
from bq.defs.log_category_base import log_category_base


########################################################################################################################
#                                  Code Generated By Categories                                                        #
########################################################################################################################

class test_category_log_category_base(log_category_base):
    def __init__(self, index):
        super().__init__(index)


class test_category_log_category_root:

    class test_category_log_ModuleA(test_category_log_category_base):
        def __init__(self):
            super().__init__(1)

        class test_category_log_SystemA(test_category_log_category_base):
            def __init__(self):
                super().__init__(2)

            class test_category_log_ClassA(test_category_log_category_base):
                def __init__(self):
                    super().__init__(3)
            ClassA = test_category_log_ClassA()
        SystemA = test_category_log_SystemA()
    ModuleA = test_category_log_ModuleA()

    class test_category_log_ModuleB(test_category_log_category_base):
        def __init__(self):
            super().__init__(4)
    ModuleB = test_category_log_ModuleB()


class test_category_log(category_log):
    _category_names = [
                            ""
                          , "ModuleA"
                          , "ModuleA.SystemA"
                          , "ModuleA.SystemA.ClassA"
                          , "ModuleB"
                          ]
    _categories_count = len(_category_names)

    def __init__(self, log_id=0):
        super().__init__(log_id)
        self.cat = test_category_log_category_root()

    @staticmethod
    def create_log(name, config):
        """
        Create a test_category_log object.

        Args:
            name: Log name. Empty string for auto-assigned name.
                  Existing name returns previous object with new config.
            config: Log configuration string.

        Returns:
            A test_category_log object. Check is_valid() to verify creation success.
        """
        log_id = category_log.call_api_create_category_log(
            name, config, test_category_log._categories_count, test_category_log._category_names)
        result = test_category_log(log_id)
        if result.get_categories_count() != test_category_log._categories_count:
            return test_category_log(0)
        return result

    @staticmethod
    def get_log_by_name(log_name):
        """
        Get a test_category_log object by its name.

        Args:
            log_name: Name of the log to find.

        Returns:
            A test_category_log object. Check is_valid() if the log was not found.
        """
        from bq.log import log
        base_log = log.get_log_by_name(log_name)
        if not base_log.is_valid():
            return test_category_log(0)
        result = test_category_log(base_log.get_id())
        if result.get_categories_count() != test_category_log._categories_count:
            return test_category_log(0)
        return result
