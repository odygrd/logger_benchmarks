/*
 * Copyright (C) 2025 Tencent.
 * BQLOG is licensed under the Apache License, Version 2.0.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 */
#include "template/category_log_template_python.h"

namespace bq {

    bq::string category_log_template_python::format(const bq::string& template_string, const category_node& root_node) const
    {
        bq::string generated_code = template_string;
        generated_code = replace_with_tab_format(generated_code, "${CLASS_NAME}", class_name_);
        generated_code = replace_with_tab_format(generated_code, "${CATEGORY_NAMES}", get_category_names_code(root_node));
        generated_code = replace_with_tab_format(generated_code, "${CATEGORY_ROOT_CLASS}", get_category_class_root_define_code(root_node));

        return generated_code;
    }

    bq::string category_log_template_python::get_category_names_code_recursive(const category_node& node) const
    {
        bq::string code = "    ";
        if (node.parent()) // empty means it is root node, first value
        {
            code += ", ";
        } else {
            code += "  ";
        }
        code += "\"" + node.full_name() + "\"\n";
        for (const auto& child : node.get_all_children()) {
            code += get_category_names_code_recursive(*child);
        }
        return code;
    }

    bq::string category_log_template_python::get_category_class_code_recursive(const category_node& node, const bq::string& tab, uint64_t& index) const
    {
        bq::string node_class_name = class_name_ + "_" + node.name();
        bq::string code = "\n" + tab + "class " + node_class_name + "(" + class_name_ + "_category_base):\n";
        code += tab + "    def __init__(self):\n";
        code += tab + "        super().__init__(" + uint64_to_string(++index) + ")\n";

        for (const auto& child : node.get_all_children()) {
            code += get_category_class_code_recursive(*child, tab + "    ", index);
        }

        code += tab + node.name() + " = " + node_class_name + "()";
        if (!node.comment().trim().is_empty()) {
            code += "  # " + node.comment();
        }
        code += "\n";
        return code;
    }

    bq::string category_log_template_python::get_category_names_code(const category_node& root_node) const
    {
        bq::string code = "[\n";
        code += get_category_names_code_recursive(root_node);
        code += "    ]";
        return code;
    }

    bq::string category_log_template_python::get_category_class_root_define_code(const category_node& root_node) const
    {
        bq::string code = "class " + class_name_ + "_category_root:\n";
        uint64_t index = 0;
        for (const auto& child : root_node.get_all_children()) {
            code += get_category_class_code_recursive(*child, "    ", index);
        }
        return code;
    }

    bq::string category_log_template_python::get_template_content() const
    {
        return R"(# Copyright (C) 2025 Tencent.
# BQLOG is licensed under the Apache License, Version 2.0.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.

"""
Generated Wrapper For ${CLASS_NAME} (Python)

A category_log that supports attaching a category to each log entry.
Categories can be used to filter logs within the appender settings.

Usage:
    my_log = ${CLASS_NAME}.create_log(name, cfg)
    my_log.info("no category")
    my_log.info(my_log.cat.ModuleA.SystemA, "with category {}", "ok")
"""

from bq.category_log import category_log
from bq.defs.log_category_base import log_category_base


########################################################################################################################
#                                  Code Generated By Categories                                                        #
########################################################################################################################

class ${CLASS_NAME}_category_base(log_category_base):
    def __init__(self, index):
        super().__init__(index)


${CATEGORY_ROOT_CLASS}

class ${CLASS_NAME}(category_log):
    _category_names = ${CATEGORY_NAMES}
    _categories_count = len(_category_names)

    def __init__(self, log_id=0):
        super().__init__(log_id)
        self.cat = ${CLASS_NAME}_category_root()

    @staticmethod
    def create_log(name, config):
        """
        Create a ${CLASS_NAME} object.

        Args:
            name: Log name. Empty string for auto-assigned name.
                  Existing name returns previous object with new config.
            config: Log configuration string.

        Returns:
            A ${CLASS_NAME} object. Check is_valid() to verify creation success.
        """
        log_id = category_log.call_api_create_category_log(
            name, config, ${CLASS_NAME}._categories_count, ${CLASS_NAME}._category_names)
        result = ${CLASS_NAME}(log_id)
        if result.get_categories_count() != ${CLASS_NAME}._categories_count:
            return ${CLASS_NAME}(0)
        return result

    @staticmethod
    def get_log_by_name(log_name):
        """
        Get a ${CLASS_NAME} object by its name.

        Args:
            log_name: Name of the log to find.

        Returns:
            A ${CLASS_NAME} object. Check is_valid() if the log was not found.
        """
        from bq.log import log
        base_log = log.get_log_by_name(log_name)
        if not base_log.is_valid():
            return ${CLASS_NAME}(0)
        result = ${CLASS_NAME}(base_log.get_id())
        if result.get_categories_count() != ${CLASS_NAME}._categories_count:
            return ${CLASS_NAME}(0)
        return result
)";
    }
}
